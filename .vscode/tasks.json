{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "jekyll run",
            "type": "shell",
            "command": "bundle exec jekyll serve --incremental",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "A custom task to run Jekyll with incremental builds"
        }
    ]
}
